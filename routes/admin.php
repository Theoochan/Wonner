<?php

declare(strict_types=1);

/**
 * Rotas do painel administrativo.
 *
 * Separadas das rotas da loja porque todas exigem usuário com papel
 * 'admin' — manter em arquivo próprio deixa visível, de relance, o que é
 * público e o que é restrito.
 *
 * @var \App\Core\Router $router
 */

// Ainda não há rotas de administração.
